# 운영 절차 — 멈춘 Case 되잡기 (sweeper)

## 무엇을 하는 것인가

접수·분류·실행을 나누면서 **그 사이에 죽으면 Case 가 중간 상태에 남는다.**

```
접수(저장·커밋) ──▶ 분류 ──▶ 201 응답 ──▶ (뒤에서) 에이전트 실행
                 ▲                      ▲
                 │                      │
        여기서 죽으면              여기서 죽으면
        `classifying` 잔류         `routing` 잔류
```

전에는 이 셋이 한 트랜잭션이라 하나가 실패하면 통째로 없던 일이 됐다. 그게 더
나빴다 — LLM 이 느리면 접수까지 막히고, 타임아웃이면 **고객이 보낸 문의가
사라졌다.** 그래서 나눴고, 나눈 대가로 이 절차가 필요해졌다.

sweeper 는 그렇게 남은 Case 를 주기적으로 훑어 **다시 처리한다.**

## 어떻게 돌리나

```bash
python -m scripts.run_sweepers --interval 60
```

| 옵션 | 언제 |
|---|---|
| `--interval 60` | 상주 실행. **이게 기본 운영 방식이다** |
| `--once` | 한 번만. cron 이나 수동 확인용 |
| `--only classifying` / `--only routing` | 한쪽만 |

수치는 `config/guardrails.yaml` 의 `reliability.*` 에 있다. 계약 문서는
`wiki/records/handoff/06_가드레일_수치.md` §4-A.

## 출력을 어떻게 읽나

한 회차마다 JSON 한 줄이 나온다.

```json
{"classifying": {"scanned": 2, "classified": 1, "failed": 1, "conflicted": 0, "errored": 0},
 "routing": {"scanned": 0, "started": 0, "errored": 0}}
```

| 칸 | 뜻 | 봐야 하나 |
|---|---|---|
| `scanned` | 멈춘 것으로 골라낸 수 | 계속 0 이 아니면 왜 자꾸 멈추는지 본다 |
| `classified` | 다시 분류해 성공 | — |
| `failed` | 분류는 실패했지만 **기록은 됐다.** Case 는 `escalated` 로 나아간다 | 급증하면 provider 상태를 본다 |
| `conflicted` | 그 사이 다른 경로가 처리했다. 정상 경합 | — |
| `started` | 실행을 다시 걸어 끝났다 | — |
| ★`errored` | **아무것도 기록하지 못했다.** Case 는 그대로 남아 다음 회차에 또 걸린다 | ★**0 이 아니면 사람이 본다** |

★`failed` 와 `errored` 를 헷갈리지 않는다. 앞은 "실패했지만 처리는 됐다",
뒤는 "아무 일도 못 했다" 이고 **할 일이 다르다.** `errored` 는 로그에
스택까지 남는다(`logger.exception`).

## `errored` 가 나오면 어떻게 알게 되나 (2026-09-07)

JSON 을 사람이 읽어야만 알 수 있던 상태였다. 이제 **밖으로 나간다.**

| 모드 | 신호 |
|---|---|
| `--once` | **exit 1.** cron 이 실패로 보고 메일·알림을 낸다 |
| `--interval` | **stderr** 한 줄. ★**죽지 않는다** |

```
★classifying sweeper: errored=2 · scanned=5 — 아무것도 기록하지 못했다. 다음 회차에 또 걸린다
```

★상주 모드가 첫 실패에 멈추면 **되잡기 자체가 멈춘다.** 멈춘 Case 를 되잡는
장치가 멈추는 것이 더 나쁘므로, 알리고 계속 돈다.

★사유는 **stderr** 로만 간다. stdout 은 JSON 한 줄이라는 계약을 지켜야
파이프로 받아 쓰는 쪽이 안 깨진다.

## 작업 스케줄러에 거는 방식 — 폐기됨 (2026-09-21)

~~`scripts/install_sweeper_task.py` 로 Windows 작업 스케줄러에 `--once` 를 주기적으로 거는 방식.~~
**2026-09-21 폐기했다. 등록돼 있던 작업(`A-COP cs sweeper`)과 등록 스크립트를 지웠다.**

| 날짜 | 일 |
|---|---|
| 2026-09-07 | 등록 스크립트 추가(스크립트 기본 주기 1분) |
| 2026-09-14 18:02 | 이 기계에 **5분 주기**로 등록됨. 등록 기록에는 계정(`playdata2`)만 남고 어느 세션이 했는지는 없다 |
| 2026-09-21 | 사용자가 "cmd 창이 계속 떴다 꺼진다"고 해서 원인을 찾아 작업·스크립트 삭제 |

### 왜 지웠나

1. **서버와 수명이 따로 논다.** sweeper 는 서버가 돌면서 남긴 멈춘 Case 를 되잡는 장치다.
   서버가 꺼져 있으면 되잡을 것이 없는데 스케줄러는 서버와 상관없이 계속 파이썬을 띄운다.
   "서버가 켜져 있을 때만 돈다"는 연결이 없었다. 위 「어떻게 돌리나」가 정한 기본 운영 방식
   (`--interval 60` 상주)과도 어긋났다.
2. **사용자가 쓰는 화면을 방해했다.** `cmd /c` 로 창을 숨기지 않고 실행해서 5분마다 검은 콘솔
   창이 떴다 사라졌다. 다른 작업을 하는 중에도 화면을 끊는다.
3. **사용자에게 묻지 않고 기계에 남는 설정을 만들었다.** 세션이 끝나도 계속 도는 반복 작업이었다.
   스크립트는 기본이 미리보기(`--apply` 를 줘야 등록)였지만, 등록 자체를 사용자와 확인하지 않았고
   끄는 법도 알리지 않았다. 사용자가 창이 계속 뜨는 것을 이상해한 뒤에야 찾았다.

★규칙으로 남긴다: **이 기계에 남는 설정(스케줄러·시작 프로그램·서비스)은 등록 전에 사용자에게
묻는다.** 창이 뜨는 방식이면 창을 숨기고(`pythonw` 등), 끄는 법을 같이 알린다.

★스케줄러 방식의 장점은 실제로 있었다 — 프로세스가 죽어도 다음 회차가 돈다, `errored` 면
exit 1 이라 스케줄러가 실패로 기록한다. 지운 이유는 그 장점이 틀려서가 아니라 위 세 가지다.
상주 `--interval` 모드는 첫 실패에 죽지 않고 stderr 로 알리지만(위 「errored 가 나오면」),
exit code 로 남는 실패 기록은 없다.

### 지금은

- 수동 확인: `python -m scripts.run_sweepers --once`. 상주: `--interval 60`.
- ★`[2026-09-22]` 상시 실행 배치(무엇이 늘 돌아야 하나 · 죽으면 무엇으로 아나)와 창 없이 거는 도구·되돌리는 법은 → [`wiki/operations/always-on.md`](../../operations/always-on.md) · [`wiki/operations/move-to-server.md`](../../operations/move-to-server.md)
- ★**서버가 켜질 때 sweeper 를 같이 띄우고 같이 내리는 배선은 아직 없다**(2026-09-21 기준, 서버
  기동 코드를 읽고 정한 것이 아니다). 붙일 때는 서버 수명에 묶는다.
- Linux cron 형태(`--once` 를 분마다)도 1번과 같은 이유로 적어 두지 않는다.
- 지운 스크립트의 백업: `scripts/_backup/2026-09-21_0029_install_sweeper_task/` (git 이력에도 있다).
- 이 문서의 수정 전 판: `wiki/records/manuals/_backup/2026-09-21_0029_운영_멈춘Case_되잡기/`.

## 자주 묻는 것

**Q. 정상 처리 중인 Case 를 되잡지 않나?**
임계값이 막는다 — 분류 5분, 실행 10분을 **넘겨야** 후보가 된다. 실행은 원래
수십 초 걸리므로(p50 20~34초) 10분은 넉넉하다. 그래도 겹치면 `expected_version`
낙관적 동시성이 하나만 통과시키고 나머지는 `conflicted` 로 센다.

**Q. 주기를 더 짧게 해도 되나?**
된다. **주기는 안전이 아니라 복구 지연을 정한다.** 안전은 임계값이 정한다.
다만 60초보다 짧게 할 실익이 없다 — 어차피 임계값을 넘어야 잡힌다.

**Q. 여러 개 띄워도 되나?**
동작은 한다(위 낙관적 동시성). 굳이 그럴 이유는 없다.

**Q. DB 부하는?**
부분 인덱스 `cases_stuck_sweep_idx`(마이그레이션 009)를 탄다 — 진행 중인 두
상태만 색인해 아주 작다. 인덱스를 넣기 전에는 테넌트의 Case 를 **매분 전부**
읽었다(EXPLAIN 으로 확인).

## 아직 없는 것

- ~~**스케줄러 배선.**~~ → 2026-09-07 작업 스케줄러 방식으로 "해결"했다고 적었으나
  **2026-09-21 폐기했다** — 이유는 위 「작업 스케줄러에 거는 방식 — 폐기됨」. **서버와 함께
  띄우는 배선은 아직 없다.** 컨테이너·systemd 배선도 여전히 없다 — 배포 형태가 정해지면 붙인다(Phase 2)
- ~~**`errored` 알림.** 로그에만 남는다~~ → **2026-09-07 해결.** exit code +
  stderr. 전용 알림 시스템(Slack·PagerDuty 등)에 붙이는 것은 그 경로가 생길 때다
