---
type: report
title: Activity — 감시 시점/횟수/주기 구조를 원본 스펙과 맞게 복원
status: done
domain: travel
---

# Activity — 감시 시점/횟수/주기 구조를 원본 스펙과 맞게 복원

## 1. 작업 목표

사용자가 `Activity_모듈_스펙.md` §3-4 원문 구조(감지 항목 표 · 하루 기준 감지 시점 표 · 검토 주기 원칙 2줄)를 다시 제시했다. 이전에 `activity.md`에 반영할 때 "재난문자API 5분 간격"(기본 폴링 주기)과 "3시간 전부터"(재검토 범위를 좁히는 규칙)를 한 표로 합쳐 두 층의 구분이 흐려져 있었다 — 이를 원본 구조대로 복원한다.

## 2. 실제 수행 내용

변경 파일: `wiki/teams/activity.md`, "TourAPI · 재난문자API 연동" 절

1. "조회 시점 · 재검토 주기" 절을 셋으로 분리:
   - "현재 장소 변경 감지 가능한 목록 (확장 예정)" — 번호·감지항목·담당API 표(운영하는지/TourAPI, 재난상황 없는지/재난문자API) 신설
   - "하루 기준 감지 시점" — 재난문자API 행을 "5분 간격 · 폴링"으로 환원(3시간 조건 제거)
   - "검토 주기 원칙" — 원본 그대로 두 문장(가까운 일정만 재검토 / 재난상황은 3시간 전부터 5분마다)
2. 두 표가 다른 층(기본 폴링 주기 vs 재검토 범위 좁히기)임을 설명하는 문단 추가, `due_activities()`가 3시간 필터를 쿼리로 구현했다는 사실과 연결

## 3. 검증 방법과 결과

문서 구조 재정리. 내부 앵커(`#조회-시점--재검토-주기`)를 참조하는 다른 절이 없음을 grep으로 확인(무결과) — 깨진 링크 없음.

## 4. 미해결 이슈·다음 작업 제안

없음 — 이번 작업은 문서 구조 정정으로 끝났다. 기존 미확보 항목(재난문자API 클라이언트 부재 등)은 변화 없음.

## 참조

- `wiki/teams/activity.md`
- `wiki/teams/Activity_모듈_스펙.md` §3-4 (원본)
