# A-COP 의존성 — 로컬에서 확인한 버전으로 고정한다 (RULE.md §1.2)
# ★2026-09-24 정정: 다섯 줄이 실제 설치판과 달랐다. 깨끗한 환경에서는 설치 자체가 실패했다
#   (pytest 7.4.4 ↔ pytest-asyncio 0.25.2 는 pytest>=8.2 를 요구 — CI 가 처음 돌면서 드러났다).
#   아래 판은 시험 1,427건이 통과한 이 환경의 실제 판이다.
# 환경: Python 3.12.7 (anaconda) / PostgreSQL 16.14 @127.0.0.1:5433 / Docker 없음

# ── 웹 · 계약 ────────────────────────────────────────────────
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.13.4
pydantic-settings==2.10.1
python-multipart==0.0.20
PyYAML==6.0.2

# ── DB (PostgreSQL + pgvector) ───────────────────────────────
sqlalchemy==2.0.41
psycopg[binary]==3.3.4
pgvector==0.5.0
alembic==1.18.4

# ── LLM · 오케스트레이션 ─────────────────────────────────────
openai==2.44.0
langgraph==1.2.8
langchain-core==1.4.9
tiktoken==0.13.0          # ★ContextPack 토큰은 추정이 아니라 실측이다

# ── MCP (read-only 3 tool) ───────────────────────────────────
mcp[cli]==1.28.1

# ── 평가 통계 (v5 §15-5) ─────────────────────────────────────
numpy==1.26.4
scipy==1.13.1             # McNemar exact
scikit-learn==1.5.2

# ── 설정 · 유틸 ──────────────────────────────────────────────
python-dotenv==1.1.1
httpx==0.28.1
socksio==1.0.0   # httpx SOCKS 프록시 — 고정 IP 서버 경유(ACOP_OUTBOUND_PROXY_URL)

# ── 테스트 ───────────────────────────────────────────────────
pytest==8.4.2
pytest-asyncio==0.25.2
