# -*- coding: utf-8 -*-
"""보내면 안 되는 알림 — 바깥으로 나가지 않았고, **보낸 것으로도 남기지 않는다**.

★`[2026-09-22]` 왜 있나. 바깥함 일꾼이 **모든 테넌트**의 통지를 집어 실제 채널로 보냈다. 점검을 한
  회차 돌리는 사이에 **시연 테넌트(`scenario-live-…`)의 통지 8건이 진짜 디스코드로 나갔다.** 받는
  사람은 그 여행의 고객 언어(대만 중국어)로 된 시연 문구를 받았다.

★`delivered` 로 찍지 않는 것이 요점이다 — 보낸 적 없는 알림을 보냈다고 적으면 그 기록이 거짓이 된다.
  `skipped` 로 사유와 함께 남기고 다시 집지 않는다.
"""
from __future__ import annotations


class NoticeSuppressed(RuntimeError):
    """이 알림은 바깥으로 보내지 않는다(보낼 대상이 아닌 테넌트 등)."""


__all__ = ["NoticeSuppressed"]
