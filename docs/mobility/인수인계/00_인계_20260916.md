# 00. 인계 — 2026-09-16 (휴가 전 마감)

**3W 산출물 4건 전부 처리했다.** 9/17 부터 휴가. 다음 세션은 4W(9/18~9/28)부터 본다.

## 오늘 낸 것 — 제출본은 docx (베이스 양식 · 이동모듈단만 채움)

| 산출물 | 제출 파일 (docx) | 바탕 md | 위치 |
|---|---|---|---|
| **데이터 전처리 결과서** | `2026-09-16_Mobility_preprocessing_report_v2.docx` | `2026-09-16_Mobility_데이터전처리결과서_v2.md` = `processed/mobility/REPORT.md` | `docs/mobility/` · 프로젝트 |
| **ML/DL 학습 결과서** | `2026-09-16_Mobility_ML_DL_training_report_v1.docx` (v1.2 · EXP-01~11) | `2026-09-16_Mobility_ML학습결과서_v1.md` | 〃 |
| **학습한 ML/DL 모델** | `2026-09-16_Mobility_trained_ML_DL_model_v1.docx` · `model_hgb.pkl`(v1) · `model_hgb_v2.pkl`(v2) | — | `docs/mobility/` · `data/travel/processed/mobility/ml_compare{,_v2}/` |
| **수집 데이터 보고서 2차** | 팀 양식에 붙일 칸별 문구 `2026-09-16_수집데이터보고서_2차_이동모듈_칸별문구_v1.md` (+변경점 v1) | 〃 | `docs/mobility/` — **팀 양식 원본에 반영은 본인이** |
| **저장소 설계 문서 2차** | **부록 A(이동)** `2026-09-16_DB_design_appendix_mobility_v1.docx` (A-0~A-4 · 13표 전부 · B/C 빈 틀) + ERD `2026-09-16_ERD_mobility_v1.{png,html}` | `2026-09-16_저장소설계문서_2차_이동모듈_칸별문구_v1.md` | `docs/mobility/` — **최연우에게 전달** (팀 문서에 부록으로 붙임) |
| ML 비교 스크립트 | `scripts/eval_ml_compare.py` (v1) · `scripts/eval_ml_compare_v2.py` (v2) · 씨앗 `tests/mobility/ml/unknown_seeds_v1.json` | | 저장소 |

docx 생성기: `out/fill_forms.py`(양식 3종) · `build_appendix.py`(부록) — 컨테이너에만 있음. 다시 만들 일 있으면 md 를 보고 손으로.

## ML 비교 — 결과 한 줄

시간표 없이 특징만으로 판정을 배우게 했더니(v1): 탐침 못 본 구간 정확도 0.848(지하철 0.992 · 버스 0.405) · 회귀 71/85 ·
**근거없음 376건을 전부 확정답으로 바꿈(376/376)** · 근거 등급 구조적으로 없음 → **판정 경로에 ML 안 넣는다.**

**v2 (한계 셋을 되물음 · EXP-08~11)**: 근거없음 구간 1→9종(탐침 11,656 · 근거없음 3,342) · 소스 존재 특징 6개 · class_weight · 격자탐색 27조합.
정확도 0.893 · 근거없음 재현율 0→0.70 까지는 오르지만 **근거없음→성립 275/3342 로 0 이 안 된다** — 남은 건 전부 UNK-07(정상 지하철+미수집 버스 환승) 한 조합. "하나라도 없으면 근거없음"은 코드 한 줄, 모델은 못 배움. 회귀 73/85. **결론 유지.**
슬롯 2(버스 소요시간 회귀)는 **설계만, 미실행** — 4W 후보.

## 저장소 설계 — 정한 것

같은 DB · **별도 스키마 `mobility`**(13표: 참조 10 · 산출 2 · 메타 1). 코어 접점은 E-17 evidences 와 논리 연결(FK 없음, 정본 M-11 / 사본 E-17).
현재 SQLite `data/travel/mobility.sqlite` → **팀 PostgreSQL 로 전환 예정**, 이식본 `sql/mobility_schema_pg.sql`. 부록 A-1·ERD 키는 이 DDL 기준(station_a/b · id identity + UNIQUE · bus_speed PK route_nm).

## 2026-09-16 재현 확인 (클라우드 컨테이너)

회귀 85/85 · 탐침 8,648 · 판단불가율 4.35% · 근거 보존율 33.5%→100% · 판정당 근거 2.91 — **전부 일치.**
synthetic 은 `mini_timetable_v2.jsonl` 로 돌려야 한다(실제 시간표면 3건 MISS).

## 다음 세션에서 할 것

1. **수집 데이터 보고서 팀 양식에 칸별 문구 반영** (본인)
2. **저장소 설계 부록 A + ERD 최연우 전달** · DDL 주석·대조 질의의 버스 행수(21/1,397 → 717/41,820) 갱신 · **sqlite 재적재** 후 `--verify-only` · PG 전환 시 `sql/mobility_schema_pg.sql` 로
3. **팀장 확인**: 학습결과서 "판정 경로 ML 없음 + 대조 실험(v1·v2)" 형태가 팀 방침과 맞는지 · 강사가 학습 모델 필수라 하면 슬롯 2 실행
4. 4W: RAG 비교(경로 B)를 `eval_ml_compare.py` 와 같은 harness 에 얹는다 — 지표·분할·실패목록 형식 그대로
5. 열린 ◆: 출력 언어(한국어 vs 모국어) · RAG 비교 규칙이 팀 1건인지 모듈별인지

## 이 방 첫 메시지

> 4W 시작. `00_인계_20260916.md` 읽고, 위 다음 할 것 1~3 부터. ML 비교 결과는 학습결과서 v1 §0 표 하나면 된다(v2 는 §5-1).
